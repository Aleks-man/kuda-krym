{
  "name": "kuda-krym",
  "version": "0.1.0",
  "private": true,
  "description": "Сервис рекомендаций пляжей Крыма по погоде, состоянию моря и времени в пути",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "npm run build --workspace @kuda-krym/contracts && npm run build --workspace @kuda-krym/database && npm run build --workspace @kuda-krym/api && npm run build --workspace @kuda-krym/web",
    "predev": "npm run build --workspace @kuda-krym/contracts",
    "dev": "concurrently --names contracts,web,api --prefix-colors yellow,cyan,green \"npm run dev --workspace @kuda-krym/contracts\" \"npm run dev --workspace @kuda-krym/web\" \"npm run dev --workspace @kuda-krym/api\"",
    "dev:web": "npm run dev --workspace @kuda-krym/web",
    "dev:api": "npm run dev --workspace @kuda-krym/api",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
