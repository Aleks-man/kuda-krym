{
  "name": "kuda-krym",
  "version": "0.1.0",
  "private": true,
  "description": "Сервис рекомендаций пляжей Крыма по погоде, состоянию моря и времени в пути",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently --names web,api --prefix-colors cyan,green \"npm run dev --workspace @kuda-krym/web\" \"npm run dev --workspace @kuda-krym/api\"",
    "dev:web": "npm run dev --workspace @kuda-krym/web",
    "dev:api": "npm run dev --workspace @kuda-krym/api",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
